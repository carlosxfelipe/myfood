name: myfood
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  card_swiper: ^3.0.1
  convex_bottom_bar: ^3.2.0
  flutter:
    sdk: flutter
  go_router: ^14.8.1
  provider: ^6.1.2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/pexels/
